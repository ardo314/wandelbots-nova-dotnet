name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x
      - name: Validate package version policy
        shell: pwsh
        run: |
          [xml] $compatibility = Get-Content -Raw eng/Compatibility.props
          ./scripts/Test-PackageVersion.ps1 -Tag "v$($compatibility.Project.PropertyGroup.NovaProductVersion)"
      - name: Generate client from committed specification
        shell: pwsh
        run: ./scripts/Generate-Client.ps1
      - run: dotnet restore
      - run: dotnet build --no-restore --configuration Release
      - run: dotnet test --no-build --configuration Release
