name: Release package

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x
      - name: Set package version
        id: version
        shell: pwsh
        run: |
          $release = ./scripts/Test-PackageVersion.ps1 -Tag $env:GITHUB_REF_NAME | ConvertFrom-Json
          "value=$($release.PackageVersion)" >> $env:GITHUB_OUTPUT
          "nova=$($release.NovaProductVersion)" >> $env:GITHUB_OUTPUT
          "openapi=$($release.OpenApiVersion)" >> $env:GITHUB_OUTPUT
          "client_revision=$($release.ClientRevision)" >> $env:GITHUB_OUTPUT
      - name: Generate client
        shell: pwsh
        run: ./scripts/Generate-Client.ps1
      - run: dotnet restore
      - run: dotnet build --no-restore --configuration Release
      - run: dotnet test --no-build --configuration Release
      - name: Pack
        run: >-
          dotnet pack src/Wandelbots.Nova.Api/Wandelbots.Nova.Api.csproj
          --no-build
          --configuration Release
          --output artifacts
          -p:PackageVersion=${{ steps.version.outputs.value }}
      - name: Publish to GitHub Packages
        run: >-
          dotnet nuget push "artifacts/*.nupkg"
          --source "https://nuget.pkg.github.com/ardo314/index.json"
          --api-key "${{ github.token }}"
          --skip-duplicate
